# Messenger
My basic messenger system used to allow communication between people in school.

## Setup
To test or run this yourself, you need a few Python modules, run `pip install requirements.txt` when you clone or download as zip

Also, you need to create users.json with the contents of `{}`
